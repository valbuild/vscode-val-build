{
  "name": "val-resolution-fixture-yarn",
  "private": true,
  "version": "0.0.0",
  "description": "Resolution fixture: a project whose only Val dependency is @valbuild/next, installed with yarn.",
  "dependencies": {
    "@valbuild/next": "^0.98.0"
  }
}
