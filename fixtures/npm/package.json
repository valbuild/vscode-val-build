{
  "name": "val-resolution-fixture-npm",
  "private": true,
  "version": "0.0.0",
  "description": "Resolution fixture: a project whose only Val dependency is @valbuild/next, installed with npm.",
  "dependencies": {
    "@valbuild/next": "^0.105.1"
  }
}
